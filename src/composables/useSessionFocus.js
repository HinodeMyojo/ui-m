import { ref } from "vue";

// Сессия занимает телефон целиком: пока она идёт, приложение прячет таб-бар и
// осенний слой. Кнопки ответа стоят в нижней трети экрана — ровно там, где
// таб-бар, — и на iPhone четвёртый вариант с «Не знаю» уходили под него.
// Листья летали по кнопкам. Флаг живёт здесь, а не в роутере: сессия
// открывается и из раздела, и с отдельного экрана /japanese/today.
export const sessionFocus = ref(false);
