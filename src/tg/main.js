import { createApp } from "vue";

import TgApp from "./TgApp.vue";
import { router } from "./router";
import { setup } from "./telegram";

// Точка входа мини-аппа Telegram.
//
// Отдельная от src/main.js нарочно: в общий бандл суперприложения входят
// задачи, бюджет, читалка и редактор диаграмм, а мини-апп должен открываться
// в метро за пару секунд. Общее у них — компоненты японского раздела, и они
// остаются в одном экземпляре, потому что репозиторий один.

// Настройка клиента идёт до создания приложения: тема и отступы под чёлку
// должны стоять уже к первому кадру.
setup();

createApp(TgApp).use(router).mount("#app");
